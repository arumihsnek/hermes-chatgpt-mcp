from __future__ import annotations

KANBAN_UI_HTML_INTERACTIVE_R16 = r'''<!DOCTYPE html>
<html lang="en" data-ui-version="interactive-r1.6.2">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'">
<meta name="referrer" content="no-referrer"><title>Hermes Kanban interactive</title>
<style>
:root{color-scheme:light dark;font:14px system-ui,sans-serif;--line:#d7dbe0;--muted:#687078;--accent:#315efb;--warn:#b7791f;--parent:#7b3ff2;--child:#0b7a75;--soft:#f7f8f9;--danger:#a61b1b;--toggle-bg:#fff;--toggle-fg:#202124;--toggle-active-bg:#e8eeff;--toggle-active-fg:#142b7a}*{box-sizing:border-box}body{margin:0;padding:10px;color:#202124;background:#fff}button,select,input,textarea{font:inherit;border:1px solid #9aa0a6;border-radius:7px;background:#fff;color:inherit}button,select,input{padding:6px 8px}button[disabled]{opacity:.42;cursor:not-allowed}textarea{padding:8px;width:100%;min-height:76px}.hidden{display:none!important}.muted{color:var(--muted);font-size:12px}.error{color:var(--danger)}.ok{color:#176b32}.row{display:flex;gap:6px;align-items:center;flex-wrap:wrap}.grow{flex:1;min-width:0}header{display:flex;align-items:center;gap:8px;flex-wrap:wrap;border-bottom:1px solid #ccd0d5;padding-bottom:8px}h1{font-size:17px;margin:0;flex:1}.banner{padding:7px 9px;border:1px solid var(--line);border-radius:7px;margin:8px 0}.status-strip{display:grid;grid-template-columns:repeat(8,minmax(0,1fr));gap:4px;width:100%;padding:7px 0 6px;overflow:visible}.status-toggle{min-width:0;padding:5px 3px;border-radius:8px;display:flex;justify-content:center;align-items:center;gap:3px;overflow:hidden;background:var(--toggle-bg);color:var(--toggle-fg)}.status-toggle .long{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.status-toggle .short{display:none}.status-toggle[aria-pressed="true"]{border-color:var(--accent);box-shadow:inset 0 0 0 1px var(--accent);background:var(--toggle-active-bg);color:var(--toggle-active-fg)}.status-toggle.dep-parent-hidden{border-color:var(--parent);box-shadow:0 0 0 1px var(--parent)}.status-toggle.dep-child-hidden{border-color:var(--child);box-shadow:0 0 0 1px var(--child)}.toggle-count{font-weight:700;flex:0 0 auto}.dep-marker{font-size:9px;font-weight:800;flex:0 0 auto}.dep-marker.parent{color:var(--parent)}.dep-marker.child{color:var(--child)}.pending-bar{position:sticky;top:0;z-index:12;display:flex;gap:7px;align-items:center;padding:7px 8px;margin:2px 0 7px;border:1px solid #8ea2ff;border-radius:9px;background:#fff;box-shadow:0 5px 16px rgba(0,0,0,.1)}.pending-bar strong{white-space:nowrap}.pending-summary{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:11px;color:var(--muted)}.confirm-primary{background:var(--accent);color:#fff;border-color:var(--accent)}.board-viewport{width:100%;overflow-x:auto;overflow-y:visible;padding:1px 0 8px;scrollbar-width:thin}.board-columns{display:grid;grid-auto-flow:column;grid-auto-columns:minmax(250px,1fr);gap:9px;align-items:start;min-width:100%}.kanban-column{min-width:250px;border:1px solid var(--line);border-radius:10px;background:var(--status-bg,var(--soft));padding:7px;min-height:130px}.kanban-column.status-triage{--status-bg:#fff8e6}.kanban-column.status-todo{--status-bg:#f7f4ee}.kanban-column.status-ready{--status-bg:#eef5ff}.kanban-column.status-running{--status-bg:#eef9f0}.kanban-column.status-blocked{--status-bg:#fff0f0}.kanban-column.status-review{--status-bg:#f4f0ff}.kanban-column.status-scheduled{--status-bg:#f0f5f7}.kanban-column.status-done{--status-bg:#f1f5f1}.kanban-column.drop-valid{border-color:var(--accent);box-shadow:inset 0 0 0 1px var(--accent)}.kanban-column.drop-invalid{opacity:.58}.kanban-column.touch-target.drop-valid{outline:3px solid var(--accent);outline-offset:2px}.kanban-column.touch-target.drop-invalid{outline:3px solid var(--danger);outline-offset:2px}.kanban-column.pending-target{border-style:dashed;border-color:var(--accent)}.column-head{display:flex;align-items:center;gap:6px;margin-bottom:7px}.column-head strong{font-size:12px}.column-count{margin-left:auto;font-size:11px;color:var(--muted)}.column-cards{display:grid;gap:6px}.card{position:relative;padding:8px;border:1px solid var(--line);border-radius:8px;background:#fff;cursor:pointer;transition:box-shadow .12s,border-color .12s,transform .12s}.card:hover{border-color:#9aa0a6;transform:translateY(-1px)}.card[draggable="true"]{cursor:grab}.card.dragging{opacity:.48;cursor:grabbing}.card.multi-selected{outline:2px solid var(--accent);outline-offset:1px}.card.touch-pressing{transform:scale(1.015);box-shadow:0 5px 16px rgba(0,0,0,.18)}.touch-ghost{position:fixed;z-index:9999;pointer-events:none;opacity:.82;max-width:260px;transform:translate(-50%,-50%) scale(1.02);box-shadow:0 8px 24px rgba(0,0,0,.24)}.selection-bar{display:flex;align-items:center;gap:7px;padding:6px 8px;margin:0 0 6px;border:1px solid var(--accent);border-radius:8px;background:var(--toggle-active-bg);color:var(--toggle-active-fg)}.selection-bar strong{flex:1}.card.dep-source{outline:2px solid var(--warn)}.card.dep-parent{border-color:var(--parent);box-shadow:0 0 0 2px color-mix(in srgb,var(--parent) 24%,transparent)}.card.dep-child{border-color:var(--child);box-shadow:0 0 0 2px color-mix(in srgb,var(--child) 24%,transparent)}.card.staged{border-style:dashed;border-color:var(--accent);background:#f7f8ff}.pending-badge{display:inline-flex;margin-top:5px;padding:2px 6px;border-radius:999px;background:#e9edff;color:#2546b8;font-size:10px;font-weight:700}.origin-badge{font-size:10px;color:var(--muted);margin-top:3px}.card-meta{font-size:11px;color:var(--muted);margin-top:3px}.dep-badge{display:inline-block;margin:5px 4px 0 0;padding:2px 5px;border-radius:999px;font-size:10px;font-weight:700}.dep-parent-badge{color:var(--parent);border:1px solid var(--parent);background:#f4efff}.dep-child-badge{color:var(--child);border:1px solid var(--child);background:#eaf8f6}.empty{padding:12px 4px;text-align:center;color:var(--muted);font-size:12px}dialog{border:0;border-radius:13px;padding:0;width:min(620px,calc(100vw - 24px));max-height:min(82vh,760px);box-shadow:0 18px 70px rgba(0,0,0,.34);background:#fff;color:inherit}dialog::backdrop{background:rgba(0,0,0,.32)}.modal-head{position:sticky;top:0;z-index:2;display:flex;gap:8px;align-items:center;padding:10px 12px;border-bottom:1px solid var(--line);background:#fff}.modal-head strong{flex:1}.modal-body{padding:11px 12px;display:grid;gap:8px;overflow:auto;max-height:calc(82vh - 52px)}.action-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px}.close-button{font-size:18px;line-height:1;padding:5px 8px}.drag-note{font-size:11px;color:var(--muted)}@media(max-width:640px){body{padding:8px}.status-strip{gap:2px}.status-toggle{font-size:10px;padding:5px 1px}.status-toggle .long{display:none}.status-toggle .short{display:inline}.board-columns{grid-auto-columns:minmax(82vw,1fr)}.kanban-column{min-width:82vw}.pending-bar{gap:4px}.pending-summary{display:none}.action-grid{grid-template-columns:1fr}}
@media(prefers-color-scheme:dark){:root{--toggle-bg:#252525;--toggle-fg:#eee;--toggle-active-bg:#303a5a;--toggle-active-fg:#fff}body,dialog,.modal-head,.pending-bar{background:#171717;color:#eee}.kanban-column{background:var(--status-bg,#202124)}.kanban-column.status-triage{--status-bg:#302b1f}.kanban-column.status-todo{--status-bg:#292722}.kanban-column.status-ready{--status-bg:#202936}.kanban-column.status-running{--status-bg:#203027}.kanban-column.status-blocked{--status-bg:#352222}.kanban-column.status-review{--status-bg:#29233a}.kanban-column.status-scheduled{--status-bg:#222c30}.kanban-column.status-done{--status-bg:#243027}.card,.status-toggle,button,select,input,textarea{background:#252525;color:#eee}.card.staged{background:#20243b}.muted,.card-meta,.origin-badge,.pending-summary{color:#b7bcc4}}
</style></head>
<body>
<header><h1>Hermes Kanban</h1><select id="board-picker" aria-label="Board"></select><button id="new-card" type="button">New card</button><button id="refresh" type="button">Refresh</button><button id="live" type="button" aria-pressed="false">Live sync</button></header>
<div id="status" class="banner muted">Connecting to canonical board…</div>
<nav id="status-strip" class="status-strip" aria-label="Visible statuses"></nav>
<div id="pending-bar" class="pending-bar hidden"><strong id="pending-count">0 pending</strong><span id="pending-summary" class="pending-summary"></span><button id="undo-all" type="button">Undo</button><button id="confirm-all" type="button" class="confirm-primary">Confirm</button></div><div id="selection-bar" class="selection-bar hidden"><strong id="selection-count">0 selected</strong><span class="muted">Long-press to select · hold + move to drag</span><button id="clear-selection" type="button">Clear selection</button></div>
<div id="board-viewport" class="board-viewport"><main id="board-columns" class="board-columns" aria-label="Kanban columns"></main></div>
<p class="drag-note">Drag cards onto supported destination columns to stage a move. Nothing mutates Hermes until Confirm.</p>
<dialog id="inspector-dialog"><div class="modal-head"><strong id="modal-title">Card</strong><button id="modal-close" class="close-button" type="button" aria-label="Close">×</button></div><div id="inspector" class="modal-body"></div></dialog>
<script>
(function(){
'use strict';
const STATUS_ORDER=['triage','todo','ready','running','blocked','review','scheduled','done'];
const LABEL={triage:'Triage',todo:'Todo',ready:'Ready',running:'In progress',blocked:'Blocked',review:'Review',scheduled:'Scheduled',done:'Done'};
const SHORT={triage:'Tri',todo:'Todo',ready:'Ready',running:'Run',blocked:'Block',review:'Rev',scheduled:'Sched',done:'Done'};
const LIVE_INTERVAL_MS=3000,MAX_LIVE_CYCLES=20,LIVE_MAX_MS=60000;
const pendingRequests=new Map();let nextRequestId=1;
const state={board:null,boards:[],counts:{},tasksByStatus:{},visible:{todo:true,ready:true,running:true},selected:null,hovered:null,selectedTask:null,graphs:{},staged:[],dragTask:null,multiSelected:new Set(),touchDrag:null,touchCleanup:null,touchAutoScrollDir:0,touchAutoScrollFrame:null,liveTimer:null,liveCycles:0,liveDeadline:0,confirming:false,refreshing:false};
const picker=document.getElementById('board-picker'),strip=document.getElementById('status-strip'),columns=document.getElementById('board-columns'),viewport=document.getElementById('board-viewport'),status=document.getElementById('status'),pendingBar=document.getElementById('pending-bar'),pendingCount=document.getElementById('pending-count'),pendingSummary=document.getElementById('pending-summary'),dialog=document.getElementById('inspector-dialog'),inspector=document.getElementById('inspector'),modalTitle=document.getElementById('modal-title');
function txt(el,v){el.textContent=v==null?'':String(v)}
function node(tag,cls,v){const el=document.createElement(tag);if(cls)el.className=cls;if(v!=null)txt(el,v);return el}
function setStatus(v,kind){status.className='banner '+(kind||'muted');txt(status,v)}
function selectedBoard(){return picker.value||state.board||null}
function request(method,params){const id=nextRequestId++;window.parent.postMessage({jsonrpc:'2.0',id,method,params},'*');return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{pendingRequests.delete(id);reject(new Error(method+' timed out waiting for MCP Apps host'))},15000);pendingRequests.set(id,{resolve,reject,timer})})}
async function tool(name,args){const result=await request('tools/call',{name,arguments:args||{}});return result&&result.structuredContent!==undefined?result.structuredContent:result}
window.addEventListener('message',(event)=>{if(event.source!==window.parent)return;const message=event.data;if(!message||message.jsonrpc!=='2.0')return;if(message.id!==undefined&&pendingRequests.has(message.id)){const pending=pendingRequests.get(message.id);pendingRequests.delete(message.id);clearTimeout(pending.timer);if(message.error)pending.reject(new Error(message.error.message||'MCP Apps host error'));else pending.resolve(message.result);return}if(message.method==='ui/notifications/tool-result'){const d=message.params&&message.params.structuredContent;if(d&&d.slug&&d.task_counts){state.board=d.slug;state.counts=d.task_counts;renderStatusStrip()}}},{passive:true});
function visibleStatuses(){return STATUS_ORDER.filter(k=>!!state.visible[k])}
async function refreshBoard(){if(state.refreshing)return;state.refreshing=true;const refreshButton=document.getElementById('refresh');refreshButton.disabled=true;saveBoardScroll();setStatus('Refreshing canonical board…','muted');try{const b=selectedBoard();const board=await tool('get_board',{request:{board:b}});state.board=board.slug||b;state.counts=board.task_counts||{};renderStatusStrip();await Promise.all(visibleStatuses().map(loadStatus));if(state.selected)await loadTask(state.selected,false);restoreBoardScroll();setStatus('Canonical board refreshed · '+new Date().toLocaleTimeString(),'ok')}finally{state.refreshing=false;refreshButton.disabled=false}}
async function loadBoards(){const data=await tool('list_boards',{});const items=(data&&data.items)||[];state.boards=items;picker.replaceChildren();items.forEach(it=>{const o=node('option',null,it.name||it.slug);o.value=it.slug;picker.appendChild(o)});const wanted=state.board||(data&&data.default_board)||(items[0]&&items[0].slug);if(wanted){picker.value=wanted;state.board=wanted}}
async function loadStatus(k){const d=await tool('list_tasks',{request:{board:selectedBoard(),status:k,limit:100,order_by:'priority'}});state.tasksByStatus[k]=(d&&d.items)||[];renderColumns()}
async function loadTask(id,open=true){const t=await tool('get_task',{request:{board:selectedBoard(),task_id:id}});state.selected=id;state.selectedTask=t;renderInspector(t);if(open){modalTitle.textContent=t.title||id;if(!dialog.open)dialog.showModal()}ensureGraph(id)}

// Scroll position preservation
let boardScrollLeft = 0;
function saveBoardScroll(){const vp=document.getElementById('board-viewport');if(vp){boardScrollLeft=vp.scrollLeft;}}
function restoreBoardScroll(){const vp=document.getElementById('board-viewport');if(vp){vp.scrollLeft=boardScrollLeft;}}

async function ensureGraph(id){if(!id||state.graphs[id])return;try{const d=await tool('get_task_graph',{request:{board:selectedBoard(),task_id:id,depth:1,max_nodes:64}});const root=(d.nodes||[]).find(n=>n.id===d.root_task_id);const deps=[];(root&&root.parents||[]).forEach(x=>deps.push({id:x.id,status:x.status,kind:'parent'}));(root&&root.children||[]).forEach(x=>deps.push({id:x.id,status:x.status,kind:'child'}));state.graphs[id]=deps;applyDependencyHighlight(state.selected||state.hovered)}catch(e){setStatus('Dependency read failed: '+(e.message||'host error'),'error')}}
function renderStatusStrip(){strip.replaceChildren();STATUS_ORDER.forEach(k=>{const b=node('button','status-toggle');b.type='button';b.dataset.status=k;b.title=LABEL[k];b.setAttribute('aria-pressed',String(!!state.visible[k]));const l=node('span','long',LABEL[k]),s=node('span','short',SHORT[k]),c=node('span','toggle-count',state.counts[k]||0);b.append(l,s,c);b.onclick=async()=>{state.visible[k]=!state.visible[k];b.setAttribute('aria-pressed',String(!!state.visible[k]));if(state.visible[k]&&!state.tasksByStatus[k])await loadStatus(k);renderColumns();applyDependencyHighlight(state.selected)};strip.appendChild(b)});applyDependencyHighlight(state.selected)}
function actionForTask(id,kind){return state.staged.find(a=>a.taskId===id&&a.kind===kind)||null}
function transitionForTask(id){return actionForTask(id,'transition')}
function stage(action){action.id=action.id||crypto.randomUUID();if(action.kind==='transition'&&action.taskId)state.staged=state.staged.filter(a=>!(a.taskId===action.taskId&&a.kind==='transition'));state.staged.push(action);renderPending();renderColumns();if(state.selectedTask)renderInspector(state.selectedTask);setStatus(action.label+' staged · Confirm to mutate Hermes.','muted')}
function undo(id){state.staged=state.staged.filter(a=>a.id!==id);renderPending();renderColumns();if(state.selectedTask)renderInspector(state.selectedTask)}
function renderPending(){pendingBar.classList.toggle('hidden',state.staged.length===0);txt(pendingCount,state.staged.length+' pending');txt(pendingSummary,state.staged.map(a=>a.label).join(' · '));document.getElementById('confirm-all').disabled=state.confirming||!state.staged.length;document.getElementById('undo-all').disabled=state.confirming||!state.staged.length}
function canonicalTasks(){const out={};STATUS_ORDER.forEach(k=>out[k]=((state.tasksByStatus[k]||[]).map(t=>Object.assign({},t))));return out}
function projectedTasks(){const out=canonicalTasks();state.staged.filter(a=>a.kind==='transition'&&a.targetStatus).forEach(a=>{let found=null,origin=null;STATUS_ORDER.some(k=>{const ix=out[k].findIndex(t=>(t.id||t.task_id)===a.taskId);if(ix>=0){found=out[k].splice(ix,1)[0];origin=k;return true}return false});if(found){found._pending=a;found._origin=origin;(out[a.targetStatus]||(out[a.targetStatus]=[])).unshift(found)}});return out}
function cardById(id){return columns.querySelector('.card[data-task-id="'+CSS.escape(id)+'"]')}
function clearDependencyHighlights(){columns.querySelectorAll('.card').forEach(c=>c.classList.remove('dep-source','dep-parent','dep-child'));strip.querySelectorAll('.status-toggle').forEach(b=>{b.classList.remove('dep-parent-hidden','dep-child-hidden');b.querySelectorAll('.dep-marker').forEach(m=>m.remove())})}
function applyDependencyHighlight(id){clearDependencyHighlights();if(!id)return;const src=cardById(id);if(src)src.classList.add('dep-source');const hidden={};(state.graphs[id]||[]).forEach(dep=>{if(state.visible[dep.status]){const c=cardById(dep.id);if(c)c.classList.add(dep.kind==='parent'?'dep-parent':'dep-child')}else{hidden[dep.status]=hidden[dep.status]||{parent:0,child:0};hidden[dep.status][dep.kind]++}});Object.entries(hidden).forEach(([k,v])=>{const b=strip.querySelector('.status-toggle[data-status="'+k+'"]');if(!b)return;if(v.parent){b.classList.add('dep-parent-hidden');b.appendChild(node('span','dep-marker parent','↑'+v.parent))}if(v.child){b.classList.add('dep-child-hidden');b.appendChild(node('span','dep-marker child','↓'+v.child))}})}
function canDrop(task,target){const st=task.status||task._origin||'';if(st==='done'||target===st)return false;if(target==='blocked'&&st!=='blocked')return true;if(target==='review'&&st!=='review'&&st!=='blocked')return true;return false}
function transitionAction(task,target){const id=task.id||task.task_id;if(target==='blocked')return{kind:'transition',taskId:id,targetStatus:'blocked',label:(task.title||id)+' → Blocked',tool:'block_tasks',args:{request:{board:selectedBoard(),task_ids:[id],kind:'needs_input',reason:'Blocked from MCP Apps UI'}}};if(target==='review')return{kind:'transition',taskId:id,targetStatus:'review',label:(task.title||id)+' → Review',tool:'request_review',args:{request:{board:selectedBoard(),task_id:id,summary:'Requested from MCP Apps UI',reviewer:'reviewer'}}};return null}
function dependencyCounts(t){const id=t.id||t.task_id;const graph=state.graphs[id]||[];const parents=Array.isArray(t.parent_ids)?t.parent_ids.length:(graph.length?graph.filter(d=>d.kind==='parent').length:'?');const children=Array.isArray(t.child_ids)?t.child_ids.length:(graph.length?graph.filter(d=>d.kind==='child').length:'?');return {parent:parents,child:children}}
function renderColumns(){saveBoardScroll();const projected=projectedTasks();columns.replaceChildren();visibleStatuses().forEach(k=>{const col=node('section','kanban-column status-'+k);col.dataset.status=k;const head=node('div','column-head');head.append(node('strong',null,LABEL[k]),node('span','column-count',String((projected[k]||[]).length)));const cards=node('div','column-cards');const items=projected[k]||[];if(!items.length)cards.appendChild(node('div','empty','No cards'));items.forEach(t=>{const id=t.id||t.task_id,card=node('article','card');card.dataset.taskId=id;card.draggable=true;const title=node('strong',null,t.title||id),meta=node('div','card-meta',(t.assignee||'unassigned')+' · '+id);const deps=dependencyCounts(t);const parentBadge=node('span','dep-badge dep-parent-badge','↑ '+deps.parent);parentBadge.title='Parent dependencies';parentBadge.setAttribute('aria-label','Parent dependencies: '+deps.parent);const childBadge=node('span','dep-badge dep-child-badge','↓ '+deps.child);childBadge.title='Child dependencies';childBadge.setAttribute('aria-label','Child dependencies: '+deps.child);card.append(title,meta,parentBadge,childBadge);const pending=t._pending||transitionForTask(id);if(pending){card.classList.add('staged');card.appendChild(node('span','pending-badge','Pending → '+(LABEL[pending.targetStatus]||pending.label)));if(t._origin)card.appendChild(node('div','origin-badge','from '+LABEL[t._origin]))}if(state.multiSelected.has(id))card.classList.add('multi-selected');card.onclick=e=>{if(card._suppressClick){card._suppressClick=false;e.preventDefault();return}loadTask(id,true)};bindTouchGestures(card,t,id);card.onpointerenter=()=>{state.hovered=id;ensureGraph(id);applyDependencyHighlight(state.selected||id)};card.onpointerleave=()=>{state.hovered=null;applyDependencyHighlight(state.selected)};card.ondragstart=e=>{state.dragTask=t;card.classList.add('dragging');e.dataTransfer.effectAllowed='move';e.dataTransfer.setData('text/plain',id);markDropTargets(t)};card.ondragend=()=>{state.dragTask=null;card.classList.remove('dragging');clearDropTargets()};cards.appendChild(card)});col.append(head,cards);col.ondragover=e=>{if(state.dragTask&&canDropDrag(state.dragTask,k)){e.preventDefault();e.dataTransfer.dropEffect='move'}};col.ondrop=e=>{if(!state.dragTask)return;e.preventDefault();stageTouchDrop(state.dragTask,k);clearDropTargets()};columns.appendChild(col)});applyDependencyHighlight(state.selected);restoreBoardScroll()}
function dragTasksFor(task){const touched=task.id||task.task_id;if(!state.multiSelected.has(touched)||state.multiSelected.size<2)return [task];return Array.from(state.multiSelected).map(taskById).filter(Boolean)}
function canDropDrag(task,target){const touched=task.id||task.task_id;const ids=state.multiSelected.has(touched)&&state.multiSelected.size>1?Array.from(state.multiSelected):[touched];const tasks=dragTasksFor(task);return tasks.length===ids.length&&tasks.length>0&&tasks.every(x=>canDrop(x,target))}
function markDropTargets(task){columns.querySelectorAll('.kanban-column').forEach(c=>c.classList.add(canDropDrag(task,c.dataset.status)?'drop-valid':'drop-invalid'))}
function clearDropTargets(){columns.querySelectorAll('.kanban-column').forEach(c=>c.classList.remove('drop-valid','drop-invalid','touch-target'))}
function allTasks(){const out=[];STATUS_ORDER.forEach(k=>(state.tasksByStatus[k]||[]).forEach(t=>out.push(t)));return out}
function taskById(id){return allTasks().find(t=>(t.id||t.task_id)===id)||null}
function renderSelection(){const bar=document.getElementById('selection-bar'),count=document.getElementById('selection-count');bar.classList.toggle('hidden',state.multiSelected.size===0);txt(count,state.multiSelected.size+' selected')}
function toggleMulti(id){if(state.multiSelected.has(id))state.multiSelected.delete(id);else state.multiSelected.add(id);renderSelection();renderColumns();applyDependencyHighlight(state.selected)}
function touchTargetAt(x,y){const el=document.elementFromPoint(x,y);return el&&el.closest?el.closest('.kanban-column'):null}
function stopTouchAutoScroll(){state.touchAutoScrollDir=0;if(state.touchAutoScrollFrame!==null){cancelAnimationFrame(state.touchAutoScrollFrame);state.touchAutoScrollFrame=null}}
function touchAutoScrollTick(){state.touchAutoScrollFrame=null;if(!state.touchDrag||!state.touchAutoScrollDir)return;const step=16;viewport.scrollLeft=Math.max(0,viewport.scrollLeft+(state.touchAutoScrollDir*step));state.touchAutoScrollFrame=requestAnimationFrame(touchAutoScrollTick)}
function autoScrollTouch(x){const r=viewport.getBoundingClientRect(),edge=36;state.touchAutoScrollDir=x<r.left+edge?-1:(x>r.right-edge?1:0);if(!state.touchAutoScrollDir){stopTouchAutoScroll();return}if(state.touchAutoScrollFrame===null)state.touchAutoScrollFrame=requestAnimationFrame(touchAutoScrollTick)}
function cleanupTouchDrag(card,pointerId){stopTouchAutoScroll();clearDropTargets();card.draggable=true;card.classList.remove('dragging','touch-pressing');try{if(card.hasPointerCapture&&card.hasPointerCapture(pointerId))card.releasePointerCapture(pointerId)}catch(_){ }state.touchDrag=null;state.dragTask=null}
function stageTouchDrop(t,target){const touched=t.id||t.task_id;const ids=state.multiSelected.has(touched)&&state.multiSelected.size>1?Array.from(state.multiSelected):[touched];const tasks=ids.map(taskById).filter(Boolean);const eligible=tasks.filter(x=>canDrop(x,target));const skipped=ids.length-eligible.length;if(ids.length>1&&skipped){setStatus('Bulk move rejected · eligible '+eligible.length+' / skipped '+skipped+' · all selected cards must share the same legal destination.','error');return}if(!eligible.length){setStatus('Move rejected · eligible 0 / skipped '+Math.max(1,skipped)+' · destination is not legal.','error');return}eligible.forEach(task=>{const a=transitionAction(task,target);if(a)stage(a)});setStatus((eligible.length>1?'Bulk staged':'Move staged')+' · eligible '+eligible.length+' / skipped '+skipped+' · Confirm to mutate Hermes.','muted')}
function bindTouchGestures(card,t,id){let press=null;const cancel=()=>{if(!press)return;clearTimeout(press.timer);cleanupTouchDrag(card,press.pointerId);press=null;state.touchCleanup=null};card.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse')return;card.draggable=false;press={pointerId:e.pointerId,x:e.clientX,y:e.clientY,long:false,dragging:false,cancelled:false,timer:setTimeout(()=>{if(!press||press.cancelled)return;press.long=true;card.classList.add('touch-pressing')},420)};state.touchCleanup=cancel},{passive:true});card.addEventListener('pointermove',e=>{if(!press||e.pointerId!==press.pointerId)return;const dist=Math.hypot(e.clientX-press.x,e.clientY-press.y);if(!press.long){if(dist>10){clearTimeout(press.timer);press.cancelled=true}return}if(!press.dragging&&dist>8){press.dragging=true;card.classList.add('dragging');state.dragTask=t;state.touchDrag={task:t,pointerId:e.pointerId,card};try{card.setPointerCapture(e.pointerId)}catch(_){ }markDropTargets(t)}if(press.dragging){e.preventDefault();autoScrollTouch(e.clientX);columns.querySelectorAll('.kanban-column').forEach(c=>c.classList.remove('touch-target'));const col=touchTargetAt(e.clientX,e.clientY);if(col)col.classList.add('touch-target')}},{passive:false});const finish=e=>{if(!press||e.pointerId!==press.pointerId)return;clearTimeout(press.timer);card._suppressClick=true;if(press.dragging){const col=touchTargetAt(e.clientX,e.clientY);if(col)stageTouchDrop(t,col.dataset.status);cleanupTouchDrag(card,e.pointerId)}else if(press.long&&!press.cancelled){toggleMulti(id);card.classList.remove('touch-pressing')}else if(!press.cancelled){loadTask(id,true)}card.draggable=true;press=null;state.touchCleanup=null};card.addEventListener('pointerup',finish);card.addEventListener('pointercancel',cancel)}
window.addEventListener('blur',()=>{if(state.touchCleanup)state.touchCleanup()});
window.addEventListener('keydown',e=>{if(e.key==='Escape'&&state.touchCleanup)state.touchCleanup()});
function button(label,fn,disabled=false){const b=node('button',null,label);b.type='button';b.disabled=disabled;b.onclick=fn;return b}
function renderInspector(t){inspector.replaceChildren();if(!t){inspector.appendChild(node('p','muted','Card not loaded.'));return}const id=t.id||t.task_id;modalTitle.textContent=t.title||id;inspector.appendChild(node('div','muted',id+' · '+(t.status||'')+' · '+(t.assignee||'unassigned')));if(t.body)inspector.appendChild(node('p',null,t.body));const stagedHere=state.staged.filter(a=>a.taskId===id);stagedHere.forEach(a=>{const r=node('div','banner',a.label+' pending');r.appendChild(button('Undo',()=>undo(a.id)));inspector.appendChild(r)});const reason=node('input','grow');reason.placeholder='Reason / review summary';inspector.appendChild(reason);const actions=node('div','action-grid');if(t.status!=='blocked'&&t.status!=='done')actions.appendChild(button('Block',()=>{const a=transitionAction(t,'blocked');a.args.request.reason=reason.value.trim()||'Blocked from MCP Apps UI';stage(a)}));if(t.status!=='review'&&t.status!=='blocked'&&t.status!=='done')actions.appendChild(button('Review',()=>{const a=transitionAction(t,'review');a.args.request.summary=reason.value.trim()||'Requested from MCP Apps UI';stage(a)}));if(t.status==='blocked')actions.appendChild(button('Unblock',()=>stage({kind:'transition',taskId:id,label:(t.title||id)+' → Unblock',tool:'unblock_tasks',args:{request:{board:selectedBoard(),task_ids:[id],reason:reason.value.trim()||'Unblocked from MCP Apps UI'}}})));if(t.status==='review')actions.appendChild(button('Changes',()=>{const v=reason.value.trim();if(!v){setStatus('Reason is required for request changes.','error');return}stage({kind:'transition',taskId:id,label:(t.title||id)+' → Changes requested',tool:'request_changes',args:{request:{board:selectedBoard(),task_id:id,reason:v}}})}));if(t.status==='todo')actions.appendChild(button('Reopen review',()=>stage({kind:'transition',taskId:id,targetStatus:'review',label:(t.title||id)+' → Reopen review',tool:'reopen_review',args:{request:{board:selectedBoard(),task_ids:[id],reason:reason.value.trim()||'Reopened from MCP Apps UI'}}})));inspector.appendChild(actions);const comment=node('textarea');comment.placeholder='Comment';inspector.appendChild(comment);inspector.appendChild(button('Comment',()=>{const v=comment.value.trim();if(v)stage({kind:'comment',taskId:id,label:'Comment on '+(t.title||id),tool:'add_comment',args:{request:{board:selectedBoard(),task_id:id,body:v}}})}));const row=node('div','row'),assignee=node('input','grow');assignee.placeholder='Assignee profile';row.append(assignee,button('Assign',()=>{const v=assignee.value.trim();if(v)stage({kind:'assign',taskId:id,label:'Assign '+(t.title||id)+' → '+v,tool:'assign_task',args:{request:{board:selectedBoard(),task_id:id,assignee:v}}})}));inspector.appendChild(row);inspector.appendChild(node('div','muted','Drag/drop stages only deterministic canonical moves: Blocked and Review. Other workflow states remain guarded by Hermes.'))}
function renderCreate(){state.selected=null;state.selectedTask=null;modalTitle.textContent='New card';inspector.replaceChildren();const title=node('input','grow');title.placeholder='Card title';const body=node('textarea');body.placeholder='Description';inspector.append(title,body,button('Create',()=>{const v=title.value.trim();if(!v){setStatus('Title is required.','error');return}stage({kind:'create',label:'Create “'+v+'”',tool:'create_task',args:{request:{board:selectedBoard(),title:v,body:body.value||null,idempotency_key:'ui-'+crypto.randomUUID()}}});dialog.close()}));if(!dialog.open)dialog.showModal()}
async function confirmAll(){if(state.confirming||!state.staged.length)return;state.confirming=true;renderPending();const queue=state.staged.slice();for(const a of queue){setStatus('Confirming '+a.label+'…','muted');try{await tool(a.tool,a.args);state.staged=state.staged.filter(x=>x.id!==a.id);renderPending();await refreshBoard()}catch(e){setStatus(a.label+' failed: '+(e.message||'host error'),'error');state.confirming=false;renderPending();return}}state.confirming=false;renderPending();setStatus('Confirmed actions reconciled from canonical board.','ok')}
function stopLive(){if(state.liveTimer)clearInterval(state.liveTimer);state.liveTimer=null;state.liveCycles=0;document.getElementById('live').setAttribute('aria-pressed','false');txt(document.getElementById('live'),'Live sync')}
function startLive(){stopLive();state.liveDeadline=Date.now()+LIVE_MAX_MS;document.getElementById('live').setAttribute('aria-pressed','true');txt(document.getElementById('live'),'Live 60s');state.liveTimer=setInterval(async()=>{if(document.hidden)return;if(Date.now()>state.liveDeadline||state.liveCycles>=MAX_LIVE_CYCLES){stopLive();return}state.liveCycles++;try{await refreshBoard()}catch(e){setStatus('Live sync failed: '+(e.message||'host error'),'error')}},LIVE_INTERVAL_MS)}
document.getElementById('clear-selection').onclick=()=>{state.multiSelected.clear();renderSelection();renderColumns();applyDependencyHighlight(state.selected)};document.getElementById('new-card').onclick=renderCreate;document.getElementById('refresh').onclick=()=>refreshBoard().catch(e=>setStatus('Refresh failed: '+(e.message||'host error'),'error'));document.getElementById('live').onclick=()=>state.liveTimer?stopLive():startLive();document.getElementById('undo-all').onclick=()=>{if(state.confirming)return;state.staged=[];renderPending();renderColumns();if(state.selectedTask)renderInspector(state.selectedTask)};document.getElementById('confirm-all').onclick=()=>confirmAll();document.getElementById('modal-close').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});picker.onchange=async()=>{state.board=picker.value;state.tasksByStatus={};state.graphs={};state.selected=null;state.selectedTask=null;await refreshBoard()};document.addEventListener('visibilitychange',()=>{if(document.hidden&&state.liveTimer){} });window.addEventListener('unload',stopLive);
window.parent.postMessage({jsonrpc:'2.0',id:0,method:'ui/initialize',params:{protocolVersion:'2025-06-18'}},'*');
(async()=>{try{await loadBoards();await refreshBoard()}catch(e){setStatus('Initial load failed: '+(e.message||'host error'),'error')}})();
})();
</script></body></html>'''


def build_kanban_ui_interactive_r16_html() -> str:
    return KANBAN_UI_HTML_INTERACTIVE_R16
